# Vercel Postgres (Vercel 대시보드 > Storage > Postgres 생성 시 자동으로 채워짐)
DATABASE_URL=""
DIRECT_URL=""

# Vercel Blob (Vercel 대시보드 > Storage > Blob 생성 시 자동으로 채워짐)
BLOB_READ_WRITE_TOKEN=""

# NextAuth
AUTH_SECRET="openssl rand -base64 32 로 생성한 임의의 문자열"

# 관리자(선생님) 최초 계정 - seed 스크립트 실행 시 사용
ADMIN_NAME="선생님"
ADMIN_EMAIL="teacher@example.com"
ADMIN_PASSWORD="바꿔주세요1234"
